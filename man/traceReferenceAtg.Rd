% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ptc-attribution.R
\name{traceReferenceAtg}
\alias{traceReferenceAtg}
\title{Trace Reference ATG Through Comparator Isoform}
\usage{
traceReferenceAtg(
  pairs,
  structures,
  cds_metadata,
  sequences,
  ejc_threshold = 50L,
  resolve_alt_start = FALSE,
  min_alt_orf_nt = 30L
)
}
\arguments{
\item{pairs}{A data frame with \code{reference_isoform_id} and
\code{comparator_isoform_id}.}

\item{structures}{A tibble from \code{\link{parseIsoformStructures}()}.}

\item{cds_metadata}{A tibble from \code{\link{extractCdsAnnotations}()}.}

\item{sequences}{A named character vector of transcript sequences (names =
isoform IDs, values = uppercase DNA sequences in transcript orientation).}

\item{ejc_threshold}{Integer; minimum distance (nt) between stop codon and
downstream junction for NMD triggering (default 50).}

\item{resolve_alt_start}{Logical; if TRUE, when the reference ATG is not
exonic in the comparator (the former \code{ref_atg_lost} terminal case),
locate the first viable ATG in the comparator and continue the trace
from that alternative start. Default FALSE preserves legacy behavior.}

\item{min_alt_orf_nt}{Integer; minimum alt-start ORF length (nt) to count
as viable. Default 30 (10 aa).}
}
\value{
A tibble with one row per pair and columns:
\code{reference_isoform_id}, \code{comparator_isoform_id},
\code{ref_atg_genomic} (strand-aware ATG position),
\code{ref_atg_exonic_in_comp} (logical),
\code{ref_orf_length} (nt in reference),
\code{comp_orf_length} (nt in comparator from ref ATG),
\code{comp_stop_tx_pos} (transcript position of stop in comparator),
\code{n_downstream_ejc} (junctions downstream of stop),
\code{alt_start_tx_pos} (transcript position of alt ATG when used; NA
otherwise), \code{alt_start_orf_length} (nt; same condition),
\code{category} (one of: "effectively_ptc", "truncated_no_ejc",
"ref_atg_lost", "no_downstream_ejc", "no_ref_cds", "mapping_failed",
and when \code{resolve_alt_start = TRUE}:
"alt_start_effectively_ptc", "alt_start_no_downstream_ejc",
"ref_atg_lost_no_viable_start").
}
\description{
For each isoform pair, checks whether the reference isoform's CDS start ATG
is exonic in the comparator, traces the reading frame to the first in-frame
stop codon, and counts downstream exon-exon junctions. This identifies
"hidden PTCs" — comparator isoforms that appear PTC-negative under their
own CDS prediction but are effectively PTC-positive when analyzed from the
reference reading frame.
}
\details{
\strong{Note on per-ORF semantics.} This function evaluates exactly \strong{one ORF
per (reference, comparator) pair} — the ORF defined by the reference
isoform's ATG traced through the comparator's exons (with optional
alt-start fallback via \code{resolve_alt_start}). The returned \code{category}
describes the fate of that single ORF, not of the comparator transcript as
a whole. A transcript can host multiple ORFs (main CDS, uORFs, alt-start
internal ORFs) with independent translation and NMD fates; use
\code{\link{enumerateOrfs}()} to enumerate every viable ORF in a set of
transcripts and get per-ORF classifications. Transcript-level "is this
NMD-targeted?" verdicts are group-by rollups over per-ORF results.
}
\examples{
# See vignette("Isopair") for a complete workflow
}
\seealso{
\code{\link{computePtcStatus}}, \code{\link{attributePtcEvents}}
}
