% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair-generation.R
\name{generatePairsDE}
\alias{generatePairsDE}
\title{Generate Isoform Pairs from Differential Expression Results}
\usage{
generatePairsDE(
  de_results,
  isoform_col,
  gene_col,
  logfc_col,
  pval_col,
  dominant_isoforms,
  pval_threshold = 0.05,
  logfc_threshold = 0
)
}
\arguments{
\item{de_results}{A data frame of differential expression results.}

\item{isoform_col}{Character; column name for isoform IDs.}

\item{gene_col}{Character; column name for gene IDs.}

\item{logfc_col}{Character; column name for log fold change.}

\item{pval_col}{Character; column name for adjusted p-value.}

\item{dominant_isoforms}{A tibble from \code{\link[=identifyDominantIsoforms]{identifyDominantIsoforms()}}
(required).}

\item{pval_threshold}{Numeric; adjusted p-value cutoff. Default 0.05.}

\item{logfc_threshold}{Numeric; absolute logFC cutoff. Default 0.}
}
\value{
A tibble with columns: gene_id, reference_isoform_id,
comparator_isoform_id, source, direction, logFC, adj_p_val.
}
\description{
Pairs each significantly differentially expressed isoform against the
dominant isoform of its gene.
}
\examples{
de_file <- system.file("extdata", "example_de.csv", package = "Isopair")
de <- read.csv(de_file)
expr_file <- system.file("extdata", "example_expression.csv",
  package = "Isopair")
expr_df <- read.csv(expr_file)
expr_mat <- as.matrix(expr_df[, -1])
rownames(expr_mat) <- expr_df$isoform_id
data(example_structures)
gene_map <- unique(example_structures[, c("isoform_id", "gene_id")])
dom <- identifyDominantIsoforms(expr_mat, gene_map, colnames(expr_mat))
pairs <- generatePairsDE(de, "isoform_id", "gene_id", "logFC",
  "adj_p_val", dom)
head(pairs)
}
