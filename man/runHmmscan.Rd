% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protein-utils.R
\name{runHmmscan}
\alias{runHmmscan}
\title{Run hmmscan and Parse Results}
\usage{
runHmmscan(
  sequences,
  hmm_database,
  n_cores = 4L,
  evalue_threshold = 1e-05,
  temp_dir = tempdir()
)
}
\arguments{
\item{sequences}{Either a named character vector (names = isoform IDs,
values = amino acid sequences) or a character path to an existing
FASTA file.}

\item{hmm_database}{Character; path to an \code{hmmpress}'d HMM database
(e.g., \code{Pfam-A.hmm}).}

\item{n_cores}{Integer; number of CPU cores for hmmscan (default 4).}

\item{evalue_threshold}{Numeric; E-value threshold for both full-sequence
and per-domain filtering (default \code{1e-5}).}

\item{temp_dir}{Character; directory for temporary files
(default \code{tempdir()}).}
}
\value{
A tibble in the same format as \code{\link{parseHmmscanOutput}()}.
}
\description{
Wrapper that runs HMMER's \code{hmmscan} on protein sequences and returns
parsed domain annotations. Requires \code{hmmscan} to be installed and
available on the system PATH.
}
\examples{
\dontrun{
seqs <- c(iso1 = "MKTLLVVFLAG...", iso2 = "MKTLLVVFLAG...")
result <- runHmmscan(seqs, "/path/to/Pfam-A.hmm")
}
}
