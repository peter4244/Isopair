% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse-structures.R
\name{parseIsoformStructures}
\alias{parseIsoformStructures}
\title{Parse Isoform Structures from a GTF File}
\usage{
parseIsoformStructures(gtf_path, isoform_ids = NULL, verbose = TRUE)
}
\arguments{
\item{gtf_path}{Path to a GTF file (can be gzipped).}

\item{isoform_ids}{Optional character vector of isoform IDs to keep.
If NULL (default), all isoforms in the GTF are returned.}

\item{verbose}{Logical; if TRUE, print progress messages.}
}
\value{
A tibble with columns: isoform_id, gene_id, chr, strand,
n_exons, exon_starts (list), exon_ends (list), tx_start, tx_end,
n_junctions.
}
\description{
Extracts exon coordinates for isoforms from a GTF annotation file and
returns a nested tibble with one row per isoform. Optionally filters to
a specific set of isoform IDs.
}
\examples{
gtf_path <- system.file("extdata", "example_small.gtf", package = "Isopair")
structures <- parseIsoformStructures(gtf_path, verbose = FALSE)
head(structures)
}
