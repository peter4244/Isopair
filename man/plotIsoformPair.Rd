% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plotIsoformPair}
\alias{plotIsoformPair}
\title{Plot an Isoform Pair Comparison}
\usage{
plotIsoformPair(
  reference_exons,
  comparator_exons,
  events,
  gene_id,
  reference_id,
  comparator_id,
  strand,
  reconstructed_exons = NULL,
  cds_metadata = NULL,
  show_events = TRUE,
  pair_label = NULL
)
}
\arguments{
\item{reference_exons}{A data frame with \code{exon_start} and \code{exon_end}
columns for the reference isoform.}

\item{comparator_exons}{A data frame with \code{exon_start} and \code{exon_end}
columns for the comparator isoform.}

\item{events}{A data frame from \code{\link[=detectEvents]{detectEvents()}} (can be 0-row or NULL).}

\item{gene_id}{Character; gene identifier for the title.}

\item{reference_id}{Character; reference isoform ID.}

\item{comparator_id}{Character; comparator isoform ID.}

\item{strand}{Character; "+" or "-".}

\item{reconstructed_exons}{Optional data frame with \code{exon_start} and
\code{exon_end} columns for the reconstructed reference isoform.}

\item{cds_metadata}{Optional data frame with columns \code{isoform_id},
\code{cds_start}, \code{cds_end}. When provided, exons are segmented and
colored by region type.}

\item{show_events}{Logical; whether to show event annotation brackets
below the comparator track. Default TRUE.}

\item{pair_label}{Optional character string for the plot title prefix.}
}
\value{
A ggplot object.
}
\description{
Visualizes the exon structures of a reference and comparator isoform pair,
optionally showing the reconstructed reference and detected splicing events.
When CDS metadata is provided, exons are colored by region type (5'UTR,
CDS, 3'UTR).
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ref <- data.frame(exon_start = c(100L, 300L, 500L),
    exon_end = c(200L, 400L, 600L))
  comp <- data.frame(exon_start = c(100L, 500L),
    exon_end = c(200L, 600L))
  events <- detectEvents(
    cbind(ref, chr = "chr1", strand = "+", gene_id = "g1",
      transcript_id = "ref"),
    cbind(comp, chr = "chr1", strand = "+", gene_id = "g1",
      transcript_id = "comp"),
    "g1", "ref", "comp", "+"
  )
  p <- plotIsoformPair(ref, comp, events, "g1", "ref", "comp", "+",
    show_events = FALSE)
  class(p) # "gg" "ggplot"
}
}
}
