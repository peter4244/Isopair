% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiles.R
\name{buildProfiles}
\alias{buildProfiles}
\title{Build Splicing Choice Profiles}
\usage{
buildProfiles(
  pairs,
  structures,
  union_exons,
  isoform_union_mapping,
  verify = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{pairs}{A data frame with columns: gene_id, reference_isoform_id,
comparator_isoform_id, and optionally strand. If strand is absent, it
is looked up from \code{structures} using the reference isoform ID.}

\item{structures}{A tibble from \code{\link[=parseIsoformStructures]{parseIsoformStructures()}} with nested
exon_starts and exon_ends.}

\item{union_exons}{Union exon tibble from \code{\link[=buildUnionExons]{buildUnionExons()}}$union_exons.}

\item{isoform_union_mapping}{Mapping tibble from
\code{\link[=buildUnionExons]{buildUnionExons()}}$isoform_union_mapping.}

\item{verify}{Logical; if TRUE (default), run reconstruction verification.}

\item{verbose}{Logical; if TRUE, print progress messages.}
}
\value{
A tibble of splicing choice profiles with one row per pair.
}
\description{
For each isoform pair, detects structural events, optionally reconstructs
and verifies the reference isoform, and builds a profile row summarizing
the structural differences.
}
\examples{
gtf_path <- system.file("extdata", "example_small.gtf", package = "Isopair")
structures <- parseIsoformStructures(gtf_path, verbose = FALSE)
ue <- buildUnionExons(structures, verbose = FALSE)
expr_file <- system.file("extdata", "example_expression.csv",
  package = "Isopair")
expr_df <- read.csv(expr_file)
expr_mat <- as.matrix(expr_df[, -1])
rownames(expr_mat) <- expr_df$isoform_id
gene_map <- unique(structures[, c("isoform_id", "gene_id")])
pairs <- generatePairsExpression(expr_mat, gene_map,
  colnames(expr_mat), method = "top_two")
profiles <- buildProfiles(pairs, structures, ue$union_exons,
  ue$isoform_union_mapping, verbose = FALSE)
}
