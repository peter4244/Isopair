% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-detection.R
\name{detectEvents}
\alias{detectEvents}
\title{Detect Structural Events Between Two Isoforms}
\usage{
detectEvents(
  reference_exons,
  comparator_exons,
  gene_id,
  reference_id,
  comparator_id,
  strand,
  tss_tolerance = 20L,
  tes_tolerance = 20L,
  splice_site_threshold = 100L
)
}
\arguments{
\item{reference_exons}{Data frame of reference isoform exons with columns:
chr, exon_start, exon_end, strand, gene_id, transcript_id.}

\item{comparator_exons}{Data frame of comparator isoform exons (same schema).}

\item{gene_id}{Gene identifier string.}

\item{reference_id}{Reference isoform ID.}

\item{comparator_id}{Comparator isoform ID.}

\item{strand}{Gene strand ("+" or "-").}

\item{tss_tolerance}{TSS change detection tolerance in bp (default 20).}

\item{tes_tolerance}{TES change detection tolerance in bp (default 20).}

\item{splice_site_threshold}{Boundary difference below this is a splice site
event (A5SS/A3SS); at or above is partial intron retention (default 100).}
}
\value{
A tibble of detected events with columns: gene_id,
reference_isoform_id, comparator_isoform_id, event_type, direction, chr,
five_prime, three_prime, strand, bp_diff, missing_terminal_exons,
orphan_terminal_exons, ir_split_exons, ref_junctions, comp_junctions.
Returns a zero-row tibble if no events detected.
}
\description{
Runs the full hierarchical event detection pipeline for a single
reference/comparator pair:
}
\details{
\enumerate{
\item Boundary determination (TSS/TES positions, genomic spans)
\item Within-boundary events (only when isoforms overlap):
IR -> Boundary (A5SS/A3SS/Partial_IR) -> SE/Missing_Internal
\item Terminal events (Alt_TSS/Alt_TES, always runs)
}
}
\examples{
# Two isoforms: reference has 3 exons, comparator skips the middle one
ref_exons <- data.frame(
  chr = "chr1", exon_start = c(100L, 300L, 500L),
  exon_end = c(200L, 400L, 600L), strand = "+",
  gene_id = "gene1", transcript_id = "tx_ref"
)
comp_exons <- data.frame(
  chr = "chr1", exon_start = c(100L, 500L),
  exon_end = c(200L, 600L), strand = "+",
  gene_id = "gene1", transcript_id = "tx_comp"
)
events <- detectEvents(ref_exons, comp_exons,
  gene_id = "gene1", reference_id = "tx_ref",
  comparator_id = "tx_comp", strand = "+"
)
events$event_type
}
