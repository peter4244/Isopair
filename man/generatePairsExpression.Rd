% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair-generation.R
\name{generatePairsExpression}
\alias{generatePairsExpression}
\title{Generate Isoform Pairs from Expression Data}
\usage{
generatePairsExpression(
  expression_matrix,
  gene_map,
  samples,
  method = "top_two",
  dominant_threshold = 0.5,
  min_expression = 0
)
}
\arguments{
\item{expression_matrix}{Numeric matrix with isoform IDs as rownames.}

\item{gene_map}{A data frame with columns \code{isoform_id} and \code{gene_id}.}

\item{samples}{Character vector of column names to use.}

\item{method}{Character; either \code{"top_two"} (pair rank 1 vs rank 2 per
gene) or \code{"dominant_vs_rest"} (pair dominant vs all others).}

\item{dominant_threshold}{Numeric; proportion threshold for dominant
isoform selection in \code{"dominant_vs_rest"} mode. Default 0.5.}

\item{min_expression}{Numeric; minimum mean expression for a comparator
isoform. Default 0 (no filter).}
}
\value{
A tibble with columns: gene_id, reference_isoform_id,
comparator_isoform_id, source, direction, logFC, adj_p_val.
}
\description{
Creates pairs of isoforms for structural comparison based on expression
ranking within each gene.
}
\examples{
expr_file <- system.file("extdata", "example_expression.csv",
  package = "Isopair")
expr_df <- read.csv(expr_file)
expr_mat <- as.matrix(expr_df[, -1])
rownames(expr_mat) <- expr_df$isoform_id
data(example_structures)
gene_map <- unique(example_structures[, c("isoform_id", "gene_id")])
pairs <- generatePairsExpression(expr_mat, gene_map, colnames(expr_mat))
head(pairs)
}
